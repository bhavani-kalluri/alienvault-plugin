openapi: 3.0.1
info:
  title: Add Sentinel Incident Comment API
  version: 1.0.0
  description: API to add a comment to a Microsoft Sentinel incident.

servers:
  - url: https://management.azure.com

paths:
  /subscriptions/{subscriptionId}/resourceGroups/{resourceGroup}/providers/Microsoft.OperationalInsights/workspaces/{workspaceName}/providers/Microsoft.SecurityInsights/incidents/{incidentId}/comments/{commentId}:
    put:
      summary: Add or update a comment on a Sentinel incident
      operationId: AddSentinelIncidentCommentV1

      parameters:
        - name: subscriptionId
          in: path
          required: true
          schema: { type: string }
        - name: resourceGroup
          in: path
          required: true
          schema: { type: string }
        - name: workspaceName
          in: path
          required: true
          schema: { type: string }
        - name: incidentId
          in: path
          required: true
          schema: { type: string }
        - name: commentId
          in: path
          required: true
          description: GUID for the comment resource
          schema: { type: string }
        - name: api-version
          in: query
          required: true
          schema:
            type: string
            enum: ["2025-07-01-preview"]

      requestBody:
        required: true
        x-ms-requestBody-name: requestBody
        content:
          application/json:
            schema:
              type: object
              properties:
                properties:
                  type: object
                  properties:
                    message:
                      type: string
                      description: Comment text to store in Sentinel
                    createdBy:
                      type: object
                      properties:
                        objectId:
                          type: string
                          description: Object ID of the App Registration or User posting the comment
                  required:
                    - message
                    - createdBy
              required:
                - properties

      responses:
        "200": { description: Comment updated successfully }
        "201": { description: Comment created successfully }
        "400": { description: Bad Request }
        "401": { description: Unauthorized }
        "403": { description: Forbidden }
        "404": { description: Not Found }
        "500": { description: Internal Server Error }

      security:
        - azure_auth:
            - https://management.azure.com/.default

components:
  securitySchemes:
    azure_auth:
      type: oauth2
      flows:
        clientCredentials:
          tokenUrl: https://login.microsoftonline.com/{tenantId}/oauth2/v2.0/token
          scopes:
            https://management.azure.com/.default: Access ARM API
